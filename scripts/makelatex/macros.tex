\usepackage[utf8]{inputenc} % specifies the charset to be used in the document
\usepackage{color}   %May be necessary if you want to color links
\usepackage[pdfusetitle]{hyperref} % Put the specified metadata (author, title, etc.)
\usepackage{tocloft} % fill with dots the toc
\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}} % for parts
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}} % for sections
\usepackage{imakeidx} % package to generate index
\usepackage{amsmath} % Package used to align math equations
\usepackage[spanish]{babel} % Using spanish as default language
\usepackage[parfill]{parskip} % Prevents indention at the beginning of a new paragraph
\usepackage{minted}
% Use custom characters in ordered list for example letters instead of numbers
\usepackage[shortlabels]{enumitem}
\usepackage{graphicx} % Package that handles images
\usepackage[margin=1in]{geometry} % Specifying the margin for all four sides
\usepackage{fontspec} % Package to change the font
\renewcommand{\baselinestretch}{1.5}
\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}
\rhead{\today}
\lhead{Roberto Carlos Aguilera Alcantar}
\rfoot{Página \thepage}
\setlength{\headheight}{15pt}

\setmainfont{Liberation Sans} % Specifying the main font of the document
% folder where images are stores
\graphicspath{ {img/} }
\decimalpoint % Use dot for separating decimal numbers
\makeindex % Making the index
\hypersetup{ % Link's color setup
    colorlinks=true, %set true if you want colored links
    linktoc=all,     %set to all if you want both sections and subsections linked
    linkcolor=black,  %choose some color if you want links to stand out
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=black
}
\setminted{
    frame=lines,
    framesep=2mm,
    baselinestretch=1.2,
    fontsize=\footnotesize,
    linenos
}

\newcommand{\mytitlepage}[3]{
    \begin{figure}[h!]
        \centering
        \includegraphics[width=2in]{/home/kasparov/Documents/uni/utl-logo.png}
    \end{figure}

    \begin{huge}
        \begin{center}
            \textbf{#1}
        \end{center}
    \end{huge}

    \begin{LARGE}

        \textbf{Alumno:} Roberto Carlos Aguilera Alcantar \\
        \textbf{Grupo:} DSM 401 \\
        \textbf{Carrera:} Desarrollo de software multiplataforma \\
        \textbf{Materia:} #2 \\
        \textbf{Profesor:} #3 \\
        \textbf{Fecha de entrega:} \today

    \end{LARGE}

    \thispagestyle{empty}
    \newpage

    \tableofcontents
    \thispagestyle{empty}
    \newpage
}

\newcommand{\mysection}[1]{
    \section*{#1}
    \addcontentsline{toc}{section}{#1}
}

\newcommand{\mysubsection}[1]{
    \subsection*{#1}
    \addcontentsline{toc}{subsection}{#1}
}

\newcommand{\mysubsubsection}[1]{
    \subsubsection*{#1}
    \addcontentsline{toc}{subsubsection}{#1}
}
